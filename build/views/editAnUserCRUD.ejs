<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit an User's information</title>
    <link rel="stylesheet" href="">

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">

    <!-- jQuery library -->
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.slim.min.js"></script>

    <!-- Popper JS -->
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<style>
    .readonly-input {
        background-color: #f0f0f0;
        /* Màu nền để biểu thị rằng trường này chỉ đọc */
        border: 1px solid #ccc;
        /* Viền của trường input */
        color: #555;
        /* Màu chữ */
        padding: 5px;
        /* Khoảng cách bên trong */
    }

    .disabled-select {
        background-color: #f0f0f0;
        /* Màu nền để biểu thị rằng trường này chỉ đọc */
        border: 1px solid #ccc;
        /* Viền của trường select */
        color: #555;
        /* Màu chữ */
        pointer-events: none;
        /* Ngăn chặn tất cả sự kiện của chuột */
    }
</style>

<body>
    <div class="container">
        <div class="row">
            <form action="/put-crud" method="POST">
                <div class="form-row">
                    <div class="col-12 mt-5"><b>Edit user's information:</b></div>
                    <div class="form-group col-6">
                        <label for="inputEmail4">Email</label>
                        <input type="email" id="readonly-field" class="form-control readonly-input"
                            value="<%= formerUserData.email%>" name="email" readonly>
                        <!-- <input type="email" class="form-control" id="email" placeholder="Email" name="email"> -->
                    </div>
                    <div class="form-group col-6">
                        <label for="inputPassword4">Password</label>
                        <input type="password" id="readonly-field" class="form-control readonly-input" id="password"
                            value="<%= formerUserData.password%>" name="password" readonly>
                        <!-- type là password thì khi nhập giao diện sẽ hiển thị chấm tròn -->
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group col-6">
                        <label for="inputEmail4">First name</label>
                        <input type="text" class="form-control" placeholder="Piscean"
                            value="<%= formerUserData.firstName%>" name="firstName" name="firstName">
                    </div>
                    <div class="form-group col-6">
                        <label for="inputPassword4">Last name</label>
                        <input type="text" class="form-control" placeholder="Phan" value="<%= formerUserData.lastName%>"
                            name="lastName">
                    </div>
                </div>


                <div class="form-group">
                    <label for="inputAddress">Address</label>
                    <input type="text" class="form-control" placeholder="khu 2 Hoành Cương Thanh Ba Phú Thọ"
                        value="<%= formerUserData.address%>" name="address">
                </div>
                <div class="form-row">
                    <div class="form-group col-6">
                        <label for="inputCity">Phone number</label>
                        <input type="text" class="form-control" value="<%= formerUserData.phoneNumber%>"
                            name="phoneNumber">
                    </div>

                    <div class="form-group col-3">
                        <label for="inputState">Sex</label>
                        <select id="inputState" class="form-control" name="gender">
                            <option value="M" <%=formerUserData.gender==='M' ? 'selected' : '' %>>Male</option>
                            <option value="F" <%=formerUserData.gender==='F' ? 'selected' : '' %>>Female</option>
                        </select>

                    </div>

                    <div class="form-group col-3">
                        <label for="inputState">Role</label>
                        <select id="inputState" class="form-control" name="roleId">
                            <option value="R3" <%=formerUserData.roleId==='R3' ? 'selected' : '' %>>Patient</option>
                            <option value="R1" <%=formerUserData.roleId==='R1' ? 'selected' : '' %>>Admin</option>
                            <option value="R2" <%=formerUserData.roleId==='R2' ? 'selected' : '' %>>Doctor</option>
                        </select>
                    </div>
                    <!-- <div class="form-group col-2">
                        <label for="inputZip">Zip</label>
                        <input type="text" class="form-control" id="inputZip">
                    </div> -->
                </div>
                <div class="form-group">
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="gridCheck">
                        <label class="form-check-label" for="gridCheck">
                            Check me out
                        </label>
                    </div>
                </div>
                <!-- mẹo để lấy nhanh Id cho file homeController, ta có hàm req.body lấy tất cả các dữ
                liệu nhập vào, vậy thì ta chỉ cần in ra trang web ô chứa id thì khi chạy câu lệnh
                req.body thì nó sẽ tự động lấy dữ liệu ở ô đó, nhưng ta nên ẩn ô này đi để đỡ lộ và xấu -->
                <input name="id" value="<%= formerUserData.id%>" type="text" hidden />
                <button type="submit" class="btn btn-primary">Save</button>
            </form>
        </div>
    </div>



</body>

</html>